1. Se for necessário criar um arquivo para um schema, verifique antes se o arquivo já existe, usando a função listFolder. Se o arquivo já
existir, verifique o conteúdo usando a função getFileContent. Se o conteúdo for muito diferente, substitua o conteúdo do arquivo pelo novo conteúdo criado. Caso contrário,
atualize conforme for necessário.
